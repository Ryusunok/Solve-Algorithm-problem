1. StringBuilder 클래스로 문자열을 쉽게 뒤집을 수 있음
2. String charAt(int i): 문자열에서 해당 인덱스에 해당하는 문자를 문자열로 변경하여 반환한다.
3. boolean equals(String another): 해당 문자열이 다른 문자열과 동일한지 체크하여 boolean값을 반환한다.
4. int length(): 해당 문자열의 길이를 반환한다.
5. int length: 배열의 길이를 반환한다.
